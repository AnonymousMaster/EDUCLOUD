<%-# SGAITECH Copyright 2013 ITECH CODE SAC-%>

<div id="content-header">
<!-- <%= image_tag("/images/admission/admission_page_logo.jpg") %> -->
  <h1><%= t('admission') %></h1>
  <h3><%= t('step4') %></h3>
</div>

<div id="page-yield">

  <% unless flash[:notice].nil? %>
  <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>


  <% form_for :student_additional_details do |a| %>
  <%#= error_messages_for 'employee_bank_details' %>

      <div id="form-contents">
      <span class="span-sub-heading"><%= t('additional_detail') %></span>
    <!-- <hr class="sub-heading"></hr> -->
          <% @additional_fields.each do |additional| %>
          <div class="form-group">
            <label><%= additional.name %></label>
            <div class="text-input-bg"><%= a.text_field :additional_info,:index => additional.id, :class => 'form-control' %></div>
            <div class="extender"></div>
          </div>
          <% end %>
      </div>

    <div class="btn-group">
      <%= submit_tag "► #{t('save_and_proceed')}" , :class => "btn btn-primary" %>
    </div> 
  <% end %>

</div>
